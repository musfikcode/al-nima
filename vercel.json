{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ],
  "env": {
    "NOTION_TOKEN": "@notion-token",
    "NOTION_DATABASE_ID": "@notion-database-id"
  }
}
